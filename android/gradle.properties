org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Flutter's default R8 code shrinking + resource shrinking for release.
# R8 full-mode obfuscation renames com.sun.jna.Pointer.peer ("peer" -> "T"),
# so JNA's JNI initIDs (GetFieldID(... "peer", "J")) fails and org.vosk.LibVosk
# throws UnsatisfiedLinkError at class-init, killing the whole process.
shrink=false
